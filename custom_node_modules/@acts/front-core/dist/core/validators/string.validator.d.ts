import { FormControl, ValidatorFn } from '@angular/forms';
import * as i0 from "@angular/core";
/**
 * Common validator imported from Chat-co robot
 *
 * @export
 * @class StringValidator
 */
export declare class StringValidator {
    /**
     * Verify length
     *
     * @static
     * @param {number} maxLength
     * @returns {ValidatorFn}
     * @memberof StringValidator
     */
    static equalLength(maxLength: number): ValidatorFn;
    /**
     * 全角チェック（全角スペースは含めない）
     *
     * @static
     * @param {FormControl} control
     * @returns {*}
     * @memberof StringValidator
     */
    static fullWidth(control: FormControl): any;
    /**
     * 全角チェック（全角スペース含む）
     *
     * @static
     * @param {FormControl} control
     * @returns {*}
     * @memberof StringValidator
     */
    static fullWidthAndSpace(control: FormControl): any;
    /**
     * 全角英数
     *
     * @static
     * @param {FormControl} control
     * @returns {*}
     * @memberof StringValidator
     */
    static fullWidthAlphabetsAndNumbers(control: FormControl): any;
    /**
     * 全角カナ
     *
     * @static
     * @param {FormControl} control
     * @returns {*}
     * @memberof StringValidator
     */
    static fullWidthKana(control: FormControl): any;
    /**
     * 全角カナ（全角スペース含む）
     *
     * @static
     * @param {FormControl} control
     * @returns {*}
     * @memberof StringValidator
     */
    static fullWidthKanaAndSpace(control: FormControl): any;
    /**
     * 全角英字
     *
     * @static
     * @param {FormControl} control
     * @returns {*}
     * @memberof StringValidator
     */
    static fullWidthAlphabets(control: FormControl): any;
    /**
     * 全角数字
     *
     * @static
     * @param {FormControl} control
     * @returns {*}
     * @memberof StringValidator
     */
    static fullWidthNumbers(control: FormControl): any;
    /**
     * 半角英数
     *
     * @static
     * @param {FormControl} control
     * @returns {*}
     * @memberof StringValidator
     */
    static halfWidthAlphabetsAndNumbers(control: FormControl): any;
    /**
     * 半角英字
     *
     * @static
     * @param {FormControl} control
     * @returns {*}
     * @memberof StringValidator
     */
    static halfWidthAlphabets(control: FormControl): any;
    /**
     * 半角カナ
     *
     * @static
     * @param {FormControl} control
     * @returns {*}
     * @memberof StringValidator
     */
    static halfWidthKana(control: FormControl): any;
    /**
     * 半角英数字と記号だけ（空白文字は入力不可）入力できるバリデーション
     *
     * @static
     * @param {FormControl} control
     * @returns {*}
     * @memberof StringValidator
     */
    static lettersAndNumbersAndSymbols(control: FormControl): any;
    /**
     * アルファベットと数字と記号が１文字以上含まれていること
     *
     * @static
     * @param {FormControl} control
     * @returns {*}
     * @memberof StringValidator
     */
    static safetyPassword(control: FormControl): any;
    static ɵfac: i0.ɵɵFactoryDef<StringValidator, never>;
    static ɵprov: i0.ɵɵInjectableDef<StringValidator>;
}
