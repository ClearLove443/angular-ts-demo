/**
 * Create Auth state
 *
 * @export
 * @param {Partial<Auth>} {
 *   uuid = '',
 *   firstName = '',
 *   lastName = '',
 *   accessToken = '',
 *   refreshToken = ''
 * }
 * @returns
 */
export function createAuth(_a) {
    var _b = _a.uuid, uuid = _b === void 0 ? '' : _b, _c = _a.firstName, firstName = _c === void 0 ? '' : _c, _d = _a.lastName, lastName = _d === void 0 ? '' : _d, _e = _a.accessToken, accessToken = _e === void 0 ? '' : _e, _f = _a.refreshToken, refreshToken = _f === void 0 ? '' : _f;
    return {
        uuid: uuid,
        firstName: firstName,
        lastName: lastName,
        accessToken: accessToken,
        refreshToken: refreshToken
    };
}
//# sourceMappingURL=auth.model.js.map