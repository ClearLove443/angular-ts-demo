/**
 * AuthCredential
 *
 * @export
 * @interface AuthCredential
 */
export interface AuthCredential {
    userId: string;
    password: string;
}
/**
 * Auth
 *
 * @export
 * @interface Auth
 */
export interface Auth {
    uuid: string;
    firstName: string;
    lastName: string;
    accessToken: string;
    refreshToken: string;
}
/**
 * Create Auth state
 *
 * @export
 * @param {Partial<Auth>} {
 *   uuid = '',
 *   firstName = '',
 *   lastName = '',
 *   accessToken = '',
 *   refreshToken = ''
 * }
 * @returns
 */
export declare function createAuth({ uuid, firstName, lastName, accessToken, refreshToken }: Partial<Auth>): Auth;
