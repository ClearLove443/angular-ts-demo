import { SemVer } from 'semver';
/**
 * Version Check Utils
 *
 * @export
 * @class VersionUtil
 */
var VersionUtil = /** @class */ (function () {
    function VersionUtil() {
    }
    /**
     * is mobile device
     *
     * @static
     * @returns {boolean}
     * @memberof DeviceUtil
     */
    VersionUtil.IS_REQUIRE = function (nowVersion, serverVersion) {
        var semVer = new SemVer(nowVersion);
        if (serverVersion === undefined ||
            serverVersion.minimumVersion === undefined ||
            semVer.compare(serverVersion.minimumVersion) >= 0) {
            return false;
        }
        return true;
    };
    /**
     * is mobile device
     *
     * @static
     * @returns {boolean}
     * @memberof DeviceUtil
     */
    VersionUtil.IS_WARNING = function (nowVersion, serverVersion) {
        var semVer = new SemVer(nowVersion);
        if (serverVersion === undefined ||
            serverVersion.latestVersion === undefined ||
            semVer.compare(serverVersion.latestVersion) >= 0) {
            return false;
        }
        return true;
    };
    return VersionUtil;
}());
export { VersionUtil };
//# sourceMappingURL=version.util.js.map