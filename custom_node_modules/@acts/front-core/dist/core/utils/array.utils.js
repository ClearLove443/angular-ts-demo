/**
 * ArrayUtils
 *
 * @export
 * @class ArrayUtils
 */
var ArrayUtils = /** @class */ (function () {
    function ArrayUtils() {
    }
    /**
     * Array Clear
     *
     * @static
     * @param {any[]} target
     * @memberof ArrayUtils
     */
    ArrayUtils.clear = function (target) {
        target.splice(0, target.length);
    };
    /**
     * Array push
     *
     * @static
     * @param {any[]} target
     * @param {any[]} elements
     * @memberof ArrayUtils
     */
    ArrayUtils.push = function (target, elements) {
        Array.prototype.push.apply(target, elements);
    };
    /**
     * Array replace
     *
     * @static
     * @param {any[]} target
     * @param {any[]} elements
     * @memberof ArrayUtils
     */
    ArrayUtils.replace = function (target, elements) {
        ArrayUtils.clear(target);
        ArrayUtils.push(target, elements);
    };
    return ArrayUtils;
}());
export { ArrayUtils };
//# sourceMappingURL=array.utils.js.map