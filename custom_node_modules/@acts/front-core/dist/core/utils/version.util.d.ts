import { CoreVersion } from '../core-state/core.model';
/**
 * Version Check Utils
 *
 * @export
 * @class VersionUtil
 */
export declare class VersionUtil {
    /**
     * is mobile device
     *
     * @static
     * @returns {boolean}
     * @memberof DeviceUtil
     */
    static IS_REQUIRE(nowVersion: string, serverVersion: CoreVersion): boolean;
    /**
     * is mobile device
     *
     * @static
     * @returns {boolean}
     * @memberof DeviceUtil
     */
    static IS_WARNING(nowVersion: string, serverVersion: CoreVersion): boolean;
}
