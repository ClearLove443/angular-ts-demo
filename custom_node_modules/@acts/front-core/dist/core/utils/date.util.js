/**
 * DateUtils
 *
 * @export
 * @class DateUtils
 */
var DateUtils = /** @class */ (function () {
    function DateUtils() {
    }
    /**
     * toISO8601DateString
     *
     * @static
     * @param {Date} date
     * @returns {string}
     * @memberof DateUtils
     */
    DateUtils.toISO8601DateString = function (date) {
        var addZero = function (time) { return ('0' + time).slice(-2); };
        return (date.getFullYear() +
            '-' +
            addZero(date.getMonth() + 1) +
            '-' +
            addZero(date.getDate()) +
            'T' +
            addZero(date.getHours()) +
            ':' +
            addZero(date.getMinutes()) +
            ':' +
            addZero(date.getSeconds()) +
            '+' +
            addZero(date.getTimezoneOffset() / -60) +
            ':00');
    };
    return DateUtils;
}());
export { DateUtils };
//# sourceMappingURL=date.util.js.map