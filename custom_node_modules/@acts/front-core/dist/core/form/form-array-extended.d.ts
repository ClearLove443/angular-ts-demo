import { AbstractControl, FormArray } from '@angular/forms';
import { ControlFactory } from './form.utils';
/**
 * Extends FormArray so it contains definition of items for further creation
 *
 * @export
 * @class FormArrayExtended
 * @extends {FormArray}
 */
export declare class FormArrayExtended extends FormArray {
    createControl: ControlFactory;
    constructor(createControl: ControlFactory, controls: AbstractControl[], ...rest: any[]);
    /**
     * set form value
     *
     * @param {any[]} value
     * @param {{ onlySelf?: boolean; emitEvent?: boolean }} [options={}]
     * @memberof FormArrayExtended
     */
    setValue(value: any[], options?: {
        onlySelf?: boolean;
        emitEvent?: boolean;
    }): void;
    /**
     * Sets specified number of controls in the array
     * @param size of the array
     */
    setSize(size: number): void;
}
