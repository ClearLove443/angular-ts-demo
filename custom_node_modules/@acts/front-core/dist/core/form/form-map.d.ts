import { AbstractControl, FormGroup } from '@angular/forms';
import { ControlFactory } from './form.utils';
/**
 * Extends FormGroup so it contains definition of map items for further creation
 *
 * @export
 * @class FormMap
 * @extends {FormGroup}
 */
export declare class FormMap extends FormGroup {
    createControl: ControlFactory;
    constructor(createControl: ControlFactory, controls: Record<string, AbstractControl>, ...rest: any[]);
    /**
     * set value to form
     *
     * @param {Record<string, any>} value
     * @param {{ onlySelf?: boolean; emitEvent?: boolean }} [options={}]
     * @memberof FormMap
     */
    setValue(value: Record<string, any>, options?: {
        onlySelf?: boolean;
        emitEvent?: boolean;
    }): void;
    /**
     * Sets child controls for a specified list of keys
     * @param keys list of keys new form group should contain
     */
    setShape(keys: string[]): void;
}
