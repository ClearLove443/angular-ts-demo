import { Subscription } from 'rxjs';
import { AuthCredential } from '../auth/auth.model';
import { AuthService } from '../auth/auth.service';
import * as i0 from "@angular/core";
/**
 * Auth service mock version
 *
 * @export
 * @class AuthServiceMock
 * @extends {AuthService}
 */
export declare class AuthServiceMock extends AuthService {
    /**
     * mock login
     *
     * @param {AuthCredential} credential
     * @returns {Subscription}
     * @memberof AuthServiceMock
     */
    login(credential: AuthCredential): Subscription;
    static ɵfac: i0.ɵɵFactoryDef<AuthServiceMock, never>;
    static ɵprov: i0.ɵɵInjectableDef<AuthServiceMock>;
}
