import { CoreService } from '../core-state/core.service';
import { InjectionUtils } from '../utils/injection.utils';
/**
 * CoreLoading decorator
 *
 * @param {boolean} show
 * @param {string} [loadingType='default']
 */
export function CoreLoading(show, loadingType) {
    if (loadingType === void 0) { loadingType = 'default'; }
    return function (_target, _name, descriptor) {
        var method = descriptor.value;
        descriptor.value = function () {
            var _this = this;
            var args = [];
            for (var _i = 0; _i < arguments.length; _i++) {
                args[_i] = arguments[_i];
            }
            var coreService = InjectionUtils.injector.get(CoreService);
            coreService.loading(show, loadingType);
            setTimeout(function () {
                method.apply(_this, args);
            });
        };
    };
}
//# sourceMappingURL=loading.js.map