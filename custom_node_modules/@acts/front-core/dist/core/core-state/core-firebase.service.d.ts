import { AngularFireAuth } from '@angular/fire/auth';
import { AngularFirestore } from '@angular/fire/firestore';
import { CoreStore } from './core.store';
import * as i0 from "@angular/core";
/**
 * Core Service
 *
 * @export
 * @class CoreService
 */
export declare class CoreFirebaseService {
    private afs;
    private afa;
    private coreStore;
    private config;
    private userConfig;
    private version;
    private maintenance;
    /**
     * Creates an instance of CoreService.
     * @param {AngularFirestore} afs
     * @param {CoreStore} coreStore
     * @memberof CoreService
     */
    constructor(afs: AngularFirestore, afa: AngularFireAuth, coreStore: CoreStore);
    /**
     *initializeFirebase
     *
     * @memberof CoreService
     */
    initializeFirebase(): void;
    /**
     * firebase login
     *
     * @public
     * @memberof CoreService
     */
    afaLogin(): void;
    /**
     * subscribe Config from firebase
     *
     * @private
     * @memberof CoreService
     */
    private fetch;
    /**
     * subscribe UserConfig from firebase
     *
     * @param {string} userId
     * @memberof CoreService
     */
    subscribeUserConfig(userId: string): void;
    static ɵfac: i0.ɵɵFactoryDef<CoreFirebaseService, never>;
    static ɵprov: i0.ɵɵInjectableDef<CoreFirebaseService>;
}
